namespace WarGame.Forms.Facade;

// Subsystem class
public class VisibilityUtils
{
    // Subsystem method
    public static bool IsVisible(bool visibility)
    {
        if (!visibility)
        {
            return false;
        }

        return true;
    }
}
